// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"cnb.cool/dtapp/certflow/ent/ca"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CA is the model entity for the CA schema.
type CA struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CA 名称
	Name string `json:"name,omitempty"`
	// ACME 目录 URL
	DirectoryURL string `json:"directory_url,omitempty"`
	// 注册邮箱
	AccountEmail string `json:"account_email,omitempty"`
	// 是否为默认 CA
	IsDefault bool `json:"is_default,omitempty"`
	// 是否启用
	IsActive bool `json:"is_active,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// 更新时间
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the CAQuery when eager-loading is set.
	Edges        CAEdges `json:"edges"`
	selectValues sql.SelectValues
}

// CAEdges holds the relations/edges for other nodes in the graph.
type CAEdges struct {
	// Certificates holds the value of the certificates edge.
	Certificates []*Certificate `json:"certificates,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// CertificatesOrErr returns the Certificates value or an error if the edge
// was not loaded in eager-loading.
func (e CAEdges) CertificatesOrErr() ([]*Certificate, error) {
	if e.loadedTypes[0] {
		return e.Certificates, nil
	}
	return nil, &NotLoadedError{edge: "certificates"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CA) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case ca.FieldIsDefault, ca.FieldIsActive:
			values[i] = new(sql.NullBool)
		case ca.FieldID:
			values[i] = new(sql.NullInt64)
		case ca.FieldName, ca.FieldDirectoryURL, ca.FieldAccountEmail:
			values[i] = new(sql.NullString)
		case ca.FieldCreatedAt, ca.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CA fields.
func (_m *CA) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case ca.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case ca.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case ca.FieldDirectoryURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field directory_url", values[i])
			} else if value.Valid {
				_m.DirectoryURL = value.String
			}
		case ca.FieldAccountEmail:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field account_email", values[i])
			} else if value.Valid {
				_m.AccountEmail = value.String
			}
		case ca.FieldIsDefault:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_default", values[i])
			} else if value.Valid {
				_m.IsDefault = value.Bool
			}
		case ca.FieldIsActive:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field is_active", values[i])
			} else if value.Valid {
				_m.IsActive = value.Bool
			}
		case ca.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case ca.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CA.
// This includes values selected through modifiers, order, etc.
func (_m *CA) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryCertificates queries the "certificates" edge of the CA entity.
func (_m *CA) QueryCertificates() *CertificateQuery {
	return NewCAClient(_m.config).QueryCertificates(_m)
}

// Update returns a builder for updating this CA.
// Note that you need to call CA.Unwrap() before calling this method if this CA
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CA) Update() *CAUpdateOne {
	return NewCAClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CA entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CA) Unwrap() *CA {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CA is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CA) String() string {
	var builder strings.Builder
	builder.WriteString("CA(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("directory_url=")
	builder.WriteString(_m.DirectoryURL)
	builder.WriteString(", ")
	builder.WriteString("account_email=")
	builder.WriteString(_m.AccountEmail)
	builder.WriteString(", ")
	builder.WriteString("is_default=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsDefault))
	builder.WriteString(", ")
	builder.WriteString("is_active=")
	builder.WriteString(fmt.Sprintf("%v", _m.IsActive))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CAs is a parsable slice of CA.
type CAs []*CA
