// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"cnb.cool/dtapp/certflow/ent/deploylog"
	"cnb.cool/dtapp/certflow/ent/deploytarget"
	"cnb.cool/dtapp/certflow/ent/predicate"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DeployLogQuery is the builder for querying DeployLog entities.
type DeployLogQuery struct {
	config
	ctx              *QueryContext
	order            []deploylog.OrderOption
	inters           []Interceptor
	predicates       []predicate.DeployLog
	withDeployTarget *DeployTargetQuery
	withFKs          bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DeployLogQuery builder.
func (_q *DeployLogQuery) Where(ps ...predicate.DeployLog) *DeployLogQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *DeployLogQuery) Limit(limit int) *DeployLogQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *DeployLogQuery) Offset(offset int) *DeployLogQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *DeployLogQuery) Unique(unique bool) *DeployLogQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *DeployLogQuery) Order(o ...deploylog.OrderOption) *DeployLogQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryDeployTarget chains the current query on the "deploy_target" edge.
func (_q *DeployLogQuery) QueryDeployTarget() *DeployTargetQuery {
	query := (&DeployTargetClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(deploylog.Table, deploylog.FieldID, selector),
			sqlgraph.To(deploytarget.Table, deploytarget.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, deploylog.DeployTargetTable, deploylog.DeployTargetColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DeployLog entity from the query.
// Returns a *NotFoundError when no DeployLog was found.
func (_q *DeployLogQuery) First(ctx context.Context) (*DeployLog, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{deploylog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *DeployLogQuery) FirstX(ctx context.Context) *DeployLog {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DeployLog ID from the query.
// Returns a *NotFoundError when no DeployLog ID was found.
func (_q *DeployLogQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{deploylog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *DeployLogQuery) FirstIDX(ctx context.Context) int {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DeployLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DeployLog entity is found.
// Returns a *NotFoundError when no DeployLog entities are found.
func (_q *DeployLogQuery) Only(ctx context.Context) (*DeployLog, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{deploylog.Label}
	default:
		return nil, &NotSingularError{deploylog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *DeployLogQuery) OnlyX(ctx context.Context) *DeployLog {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DeployLog ID in the query.
// Returns a *NotSingularError when more than one DeployLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *DeployLogQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{deploylog.Label}
	default:
		err = &NotSingularError{deploylog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *DeployLogQuery) OnlyIDX(ctx context.Context) int {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DeployLogs.
func (_q *DeployLogQuery) All(ctx context.Context) ([]*DeployLog, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DeployLog, *DeployLogQuery]()
	return withInterceptors[[]*DeployLog](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *DeployLogQuery) AllX(ctx context.Context) []*DeployLog {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DeployLog IDs.
func (_q *DeployLogQuery) IDs(ctx context.Context) (ids []int, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(deploylog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *DeployLogQuery) IDsX(ctx context.Context) []int {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *DeployLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*DeployLogQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *DeployLogQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *DeployLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *DeployLogQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DeployLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *DeployLogQuery) Clone() *DeployLogQuery {
	if _q == nil {
		return nil
	}
	return &DeployLogQuery{
		config:           _q.config,
		ctx:              _q.ctx.Clone(),
		order:            append([]deploylog.OrderOption{}, _q.order...),
		inters:           append([]Interceptor{}, _q.inters...),
		predicates:       append([]predicate.DeployLog{}, _q.predicates...),
		withDeployTarget: _q.withDeployTarget.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithDeployTarget tells the query-builder to eager-load the nodes that are connected to
// the "deploy_target" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *DeployLogQuery) WithDeployTarget(opts ...func(*DeployTargetQuery)) *DeployLogQuery {
	query := (&DeployTargetClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withDeployTarget = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CertID int `json:"cert_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DeployLog.Query().
//		GroupBy(deploylog.FieldCertID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *DeployLogQuery) GroupBy(field string, fields ...string) *DeployLogGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DeployLogGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = deploylog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CertID int `json:"cert_id,omitempty"`
//	}
//
//	client.DeployLog.Query().
//		Select(deploylog.FieldCertID).
//		Scan(ctx, &v)
func (_q *DeployLogQuery) Select(fields ...string) *DeployLogSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &DeployLogSelect{DeployLogQuery: _q}
	sbuild.label = deploylog.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DeployLogSelect configured with the given aggregations.
func (_q *DeployLogQuery) Aggregate(fns ...AggregateFunc) *DeployLogSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *DeployLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !deploylog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *DeployLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DeployLog, error) {
	var (
		nodes       = []*DeployLog{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [1]bool{
			_q.withDeployTarget != nil,
		}
	)
	if _q.withDeployTarget != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, deploylog.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DeployLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DeployLog{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withDeployTarget; query != nil {
		if err := _q.loadDeployTarget(ctx, query, nodes, nil,
			func(n *DeployLog, e *DeployTarget) { n.Edges.DeployTarget = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *DeployLogQuery) loadDeployTarget(ctx context.Context, query *DeployTargetQuery, nodes []*DeployLog, init func(*DeployLog), assign func(*DeployLog, *DeployTarget)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*DeployLog)
	for i := range nodes {
		if nodes[i].deploy_target_deploy_logs == nil {
			continue
		}
		fk := *nodes[i].deploy_target_deploy_logs
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(deploytarget.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "deploy_target_deploy_logs" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (_q *DeployLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *DeployLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(deploylog.Table, deploylog.Columns, sqlgraph.NewFieldSpec(deploylog.FieldID, field.TypeInt))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, deploylog.FieldID)
		for i := range fields {
			if fields[i] != deploylog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *DeployLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(deploylog.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = deploylog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DeployLogGroupBy is the group-by builder for DeployLog entities.
type DeployLogGroupBy struct {
	selector
	build *DeployLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *DeployLogGroupBy) Aggregate(fns ...AggregateFunc) *DeployLogGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *DeployLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeployLogQuery, *DeployLogGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *DeployLogGroupBy) sqlScan(ctx context.Context, root *DeployLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DeployLogSelect is the builder for selecting fields of DeployLog entities.
type DeployLogSelect struct {
	*DeployLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *DeployLogSelect) Aggregate(fns ...AggregateFunc) *DeployLogSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *DeployLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DeployLogQuery, *DeployLogSelect](ctx, _s.DeployLogQuery, _s, _s.inters, v)
}

func (_s *DeployLogSelect) sqlScan(ctx context.Context, root *DeployLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
