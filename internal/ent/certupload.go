// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"cnb.cool/dtapp/certflow/internal/ent/certupload"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// CertUpload is the model entity for the CertUpload schema.
type CertUpload struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 云厂商：aliyun / tencentcloud / huawei
	Provider string `json:"provider,omitempty"`
	// 用于区分云账号的 AccessKeyId（非密钥）
	AccessKeyID string `json:"access_key_id,omitempty"`
	// 区域
	Region string `json:"region,omitempty"`
	// 证书内容指纹（sha256），用于标识同一张证书
	CertFingerprint string `json:"cert_fingerprint,omitempty"`
	// 云端返回的证书 ID
	CloudCertID string `json:"cloud_cert_id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CertUpload) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case certupload.FieldID:
			values[i] = new(sql.NullInt64)
		case certupload.FieldProvider, certupload.FieldAccessKeyID, certupload.FieldRegion, certupload.FieldCertFingerprint, certupload.FieldCloudCertID:
			values[i] = new(sql.NullString)
		case certupload.FieldCreatedAt, certupload.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CertUpload fields.
func (_m *CertUpload) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case certupload.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case certupload.FieldProvider:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field provider", values[i])
			} else if value.Valid {
				_m.Provider = value.String
			}
		case certupload.FieldAccessKeyID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field access_key_id", values[i])
			} else if value.Valid {
				_m.AccessKeyID = value.String
			}
		case certupload.FieldRegion:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field region", values[i])
			} else if value.Valid {
				_m.Region = value.String
			}
		case certupload.FieldCertFingerprint:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cert_fingerprint", values[i])
			} else if value.Valid {
				_m.CertFingerprint = value.String
			}
		case certupload.FieldCloudCertID:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field cloud_cert_id", values[i])
			} else if value.Valid {
				_m.CloudCertID = value.String
			}
		case certupload.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case certupload.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the CertUpload.
// This includes values selected through modifiers, order, etc.
func (_m *CertUpload) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this CertUpload.
// Note that you need to call CertUpload.Unwrap() before calling this method if this CertUpload
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *CertUpload) Update() *CertUploadUpdateOne {
	return NewCertUploadClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the CertUpload entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *CertUpload) Unwrap() *CertUpload {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: CertUpload is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *CertUpload) String() string {
	var builder strings.Builder
	builder.WriteString("CertUpload(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("provider=")
	builder.WriteString(_m.Provider)
	builder.WriteString(", ")
	builder.WriteString("access_key_id=")
	builder.WriteString(_m.AccessKeyID)
	builder.WriteString(", ")
	builder.WriteString("region=")
	builder.WriteString(_m.Region)
	builder.WriteString(", ")
	builder.WriteString("cert_fingerprint=")
	builder.WriteString(_m.CertFingerprint)
	builder.WriteString(", ")
	builder.WriteString("cloud_cert_id=")
	builder.WriteString(_m.CloudCertID)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// CertUploads is a parsable slice of CertUpload.
type CertUploads []*CertUpload
