// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"cnb.cool/dtapp/certflow/internal/ent/authmethod"
	"cnb.cool/dtapp/certflow/internal/ent/totpcredential"
	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// TOTPCredential is the model entity for the TOTPCredential schema.
type TOTPCredential struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// TOTP 密钥（Base32 编码）
	Secret string `json:"-"`
	// 发行者名称
	Issuer string `json:"issuer,omitempty"`
	// 账户名
	AccountName string `json:"account_name,omitempty"`
	// 关联的认证方式 ID
	AuthMethodID int `json:"auth_method_id,omitempty"`
	// 创建时间
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TOTPCredentialQuery when eager-loading is set.
	Edges        TOTPCredentialEdges `json:"edges"`
	selectValues sql.SelectValues
}

// TOTPCredentialEdges holds the relations/edges for other nodes in the graph.
type TOTPCredentialEdges struct {
	// AuthMethod holds the value of the auth_method edge.
	AuthMethod *AuthMethod `json:"auth_method,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// AuthMethodOrErr returns the AuthMethod value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TOTPCredentialEdges) AuthMethodOrErr() (*AuthMethod, error) {
	if e.AuthMethod != nil {
		return e.AuthMethod, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: authmethod.Label}
	}
	return nil, &NotLoadedError{edge: "auth_method"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*TOTPCredential) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case totpcredential.FieldID, totpcredential.FieldAuthMethodID:
			values[i] = new(sql.NullInt64)
		case totpcredential.FieldSecret, totpcredential.FieldIssuer, totpcredential.FieldAccountName:
			values[i] = new(sql.NullString)
		case totpcredential.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the TOTPCredential fields.
func (_m *TOTPCredential) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case totpcredential.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case totpcredential.FieldSecret:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field secret", values[i])
			} else if value.Valid {
				_m.Secret = value.String
			}
		case totpcredential.FieldIssuer:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field issuer", values[i])
			} else if value.Valid {
				_m.Issuer = value.String
			}
		case totpcredential.FieldAccountName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field account_name", values[i])
			} else if value.Valid {
				_m.AccountName = value.String
			}
		case totpcredential.FieldAuthMethodID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field auth_method_id", values[i])
			} else if value.Valid {
				_m.AuthMethodID = int(value.Int64)
			}
		case totpcredential.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the TOTPCredential.
// This includes values selected through modifiers, order, etc.
func (_m *TOTPCredential) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryAuthMethod queries the "auth_method" edge of the TOTPCredential entity.
func (_m *TOTPCredential) QueryAuthMethod() *AuthMethodQuery {
	return NewTOTPCredentialClient(_m.config).QueryAuthMethod(_m)
}

// Update returns a builder for updating this TOTPCredential.
// Note that you need to call TOTPCredential.Unwrap() before calling this method if this TOTPCredential
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *TOTPCredential) Update() *TOTPCredentialUpdateOne {
	return NewTOTPCredentialClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the TOTPCredential entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *TOTPCredential) Unwrap() *TOTPCredential {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: TOTPCredential is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *TOTPCredential) String() string {
	var builder strings.Builder
	builder.WriteString("TOTPCredential(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("secret=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("issuer=")
	builder.WriteString(_m.Issuer)
	builder.WriteString(", ")
	builder.WriteString("account_name=")
	builder.WriteString(_m.AccountName)
	builder.WriteString(", ")
	builder.WriteString("auth_method_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AuthMethodID))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// TOTPCredentials is a parsable slice of TOTPCredential.
type TOTPCredentials []*TOTPCredential
